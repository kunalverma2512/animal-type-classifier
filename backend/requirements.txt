# Core FastAPI (using standard bundle which includes uvicorn)
fastapi[standard]
python-dotenv
pydantic-settings
python-multipart
aiofiles

# Database
motor
pymongo

# AI Integration (optional - works without)
google-generativeai

# ML Models for Cattle Analysis
# Install CPU-only PyTorch first (prevents CUDA downloads on Render)
--extra-index-url https://download.pytorch.org/whl/cpu
torch==2.1.0+cpu
torchvision==0.16.0+cpu
ultralytics
requests>=2.31.0

# Image Processing
Pillow
opencv-python-headless  # Required by ultralytics and model integration files
